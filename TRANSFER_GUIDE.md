# دليل نقل وتعديل تطبيق Joja 🌿

## نظرة عامة

هذا التطبيق مُصمم ليكون **محمول بالكامل** - يمكن نقله، بيعه، أو تعديله بسهولة تامة.

---

## نقل التطبيق لدومين/هوست جديد 🚀

### الطريقة 1: نشر على استضافة جديدة

التطبيق يعمل على **أي استضافة تدعم ASP.NET Core 8.0**:

- ✅ **Railway.app** (مجاني)
- ✅ **Azure App Service**
- ✅ **AWS Elastic Beanstalk**
- ✅ **SmarterASP.NET**
- ✅ **أي VPS** (Windows/Linux)

**خطوات النقل:**

1. ارفع الكود على الاستضافة الجديدة
2. عدّل `appsettings.Production.json` إذا لزم الأمر
3. البيانات محفوظة في `joja.db` - يمكن نسخها ونقلها

### الطريقة 2: تغيير الدومين

التطبيق يعمل على **أي دومين** بدون تعديل الكود!

```bash
# مثال: نقل من railway.app إلى دومين خاص
yoursite.com  →  يوجّه إلى التطبيق
```

---

## تعديل البراند والإعدادات ⚙️

### تغيير اسم العلامة التجارية

عدّل ملف: **`Joja.Api/appsettings.json`**

```json
{
  "BrandSettings": {
    "WhatsAppNumber": "201234567890",        // ← رقم WhatsApp
    "SupportEmail": "info@newbrand.com",    // ← البريد الإلكتروني
    "BrandName": "اسم العلامة التجارية الجديدة"  // ← الاسم
  }
}
```

### تغيير المنتجات والفئات

**الطريقة 1: عبر لوحة التحكم (قريباً)**

- يمكن إضافة صفحة إدارة للمنتجات في `/Home/ManageProducts`

**الطريقة 2: عبر قاعدة البيانات مباشرة**

استخدم أداة **DB Browser for SQLite**:

1. حمّل من: [sqlitebrowser.org](https://sqlitebrowser.org/)
2. افتح ملف `joja.db`
3. عدّل جداول `Products` و `Categories`

**الطريقة 3: تعديل Seed Data في الكود**

عدّل ملف: **`Joja.Api/Data/ApplicationDbContext.cs`**

```csharp
// Seed Categories (السطر 21)
modelBuilder.Entity<Category>().HasData(
    new Category { Id = 1, Name = "فئة جديدة 1" },
    new Category { Id = 2, Name = "فئة جديدة 2" }
);

// Seed Products (السطر 28)
modelBuilder.Entity<Product>().HasData(
    new Product 
    { 
        Id = 1, 
        Name = "منتج جديد", 
        Description = "وصف المنتج",
        Price = 100, 
        MainImageUrl = "/images/product.jpg",
        CategoryId = 1
    }
);
```

ثم:

```bash
# احذف قاعدة البيانات القديمة
Remove-Item Joja.Api/joja.db

# شغّل التطبيق من جديد (سيُنشئ قاعدة بيانات جديدة)
dotnet run --project Joja.Api
```

---

## نقل البيانات (Database) 📦

### نسخ قاعدة البيانات

قاعدة البيانات في ملف واحد: **`Joja.Api/joja.db`**

**لنقل البيانات:**

```bash
# 1. انسخ الملف
Copy-Item Joja.Api/joja.db backup-joja.db

# 2. انقل النسخة للاستضافة الجديدة
# 3. ضع الملف في مجلد التطبيق
```

### دمج البيانات من موقعين

إذا كان لديك موقعان وتريد دمج البيانات:

```bash
# استخدم SQLite command line
sqlite3 joja-merged.db

# اربط قاعدتي البيانات
ATTACH 'joja-site1.db' AS site1;
ATTACH 'joja-site2.db' AS site2;

# انسخ البيانات
INSERT INTO Products SELECT * FROM site1.Products;
INSERT INTO Products SELECT * FROM site2.Products;
```

---

## بيع التطبيق 💰

### ما تحتاج تعديله عند البيع

1. **إعدادات العلامة التجارية** (`appsettings.json`)
   - رقم WhatsApp
   - البريد الإلكتروني
   - اسم العلامة التجارية

2. **المنتجات والفئات** (قاعدة البيانات أو Seed Data)

3. **الصور** (مجلد `wwwroot/images/`)

4. **الألوان والتصميم** (اختياري)
   - `wwwroot/css/site.css`

### ملفات يجب تسليمها للمشتري

```
✅ جميع ملفات الكود (المجلد الكامل)
✅ قاعدة البيانات joja.db (إذا كانت تحتوي بيانات)
✅ هذا الدليل (TRANSFER_GUIDE.md)
✅ دليل الاستخدام
```

---

## تغيير اللغة والعملة 🌍

### تغيير العملة من جنيه إلى عملة أخرى

ابحث في الملفات عن `EGP` أو `جنيه` وغيّرها.

**مثال:**

في `HomeController.cs` السطر 63:

```csharp
// قبل
string message = $"Hello Joja, I would like to confirm my order #{order.Id}. Total: {order.TotalAmount} EGP.";

// بعد (للريال السعودي مثلاً)
string message = $"Hello Joja, I would like to confirm my order #{order.Id}. Total: {order.TotalAmount} SAR.";
```

### إضافة لغات متعددة

التطبيق حالياً يدعم العربية والإنجليزية مختلطة.

لإضافة دعم لغات كامل، يمكن استخدام:

- **ASP.NET Core Localization**
- ملفات Resources (`.resx`)

---

## الاستضافة المجانية الموصى بها ⭐

| المنصة | المميزات | السلبيات |
|--------|----------|----------|
| **Railway.app** | $5 شهرياً مجاناً، سهلة جداً | تتطلب بطاقة ائتمان للتحقق |
| **Azure Free Tier** | قوية وموثوقة | محدودة بـ 60 دقيقة CPU يومياً |
| **SmarterASP.NET** | مخصصة لـ .NET | نطاق ترددي محدود |

**الأفضل للمعاينة:** Railway.app  
**الأفضل للإنتاج:** VPS مدفوع (5-10$/شهر)

---

## نصائح مهمة 💡

> [!WARNING]
> **قبل بيع التطبيق:**
>
> - احذف أي بيانات حساسة من قاعدة البيانات
> - غيّر رقم WhatsApp والبريد الإلكتروني في `appsettings.json`
> - تأكد من حذف ملف `joja.db` إذا كان يحتوي بيانات العملاء

> [!TIP]
> **لسهولة التعديل المستقبلي:**
>
> - وثّق أي تعديلات تقوم بها
> - احتفظ بنسخة احتياطية من قاعدة البيانات دورياً
> - استخدم Git لتتبع التغييرات

---

## دعم فني ⚙️

إذا واجهت مشاكل عند النقل:

1. تأكد من تثبيت .NET 8.0 SDK
2. تأكد من صلاحيات الكتابة على مجلد قاعدة البيانات
3. راجع logs في حال حدوث أخطاء

---

**التطبيق جاهز للنقل في أي وقت! 🚀**
