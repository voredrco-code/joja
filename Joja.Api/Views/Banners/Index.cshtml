@model IEnumerable<Joja.Api.Models.Banner>

@{
    ViewData["Title"] = "Manage Banners";
}

<style>
    .draggable-row {
        cursor: move;
        user-select: none;
    }
    .draggable-row:hover {
        background-color: #f8f9fa;
    }
    .drag-handle {
        cursor: grab;
        color: #999;
        margin-right: 10px;
    }
    .drag-handle:active {
        cursor: grabbing;
    }
</style>

<div class="container" style="margin-top: 50px;">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="brand-font">Manage Banners</h1>
        <a asp-action="Create" class="btn-joja-dark">Add New Banner</a>
    </div>

    <div class="alert alert-info">
        <i class="fas fa-info-circle"></i> Drag and drop rows to reorder banners
    </div>

    <table class="table table-striped table-hover" id="bannersTable">
        <thead>
            <tr>
                <th></th>
                <th>Order</th>
                <th>Preview</th>
                <th>Type</th>
                <th>Title</th>
                <th>Subtitle</th>
                <th>Link</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody id="bannersList">
            @foreach (var item in Model) {
                <tr class="draggable-row" data-id="@item.Id">
                    <td class="drag-handle">
                        <i class="fas fa-grip-vertical"></i>
                    </td>
                    <td>@item.DisplayOrder</td>
                    <td>
                        @if (item.BannerType == "Video" && !string.IsNullOrEmpty(item.VideoUrl))
                        {
                            <video src="@item.VideoUrl" style="width: 100px; height: 50px; object-fit: cover; border-radius: 4px;" muted></video>
                        }
                        else
                        {
                            <img src="@item.ImageUrl" style="width: 100px; height: 50px; object-fit: cover; border-radius: 4px;" />
                        }
                    </td>
                    <td>
                        @if (item.BannerType == "Video")
                        {
                            <span class="badge bg-primary">Video</span>
                        }
                        else
                        {
                            <span class="badge bg-success">Image</span>
                        }
                    </td>
                    <td>@item.Title</td>
                    <td>@item.Subtitle</td>
                    <td><a href="@item.LinkUrl" target="_blank">Link</a></td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary">Edit</a>
                        <form asp-action="Delete" asp-route-id="@item.Id" method="post" style="display:inline;" onsubmit="return confirm('Are you sure?');">
                            <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    
    <div style="margin-top: 20px;">
        <a asp-controller="Admin" asp-action="Index" class="btn btn-secondary">Back to Dashboard</a>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/sortablejs@latest/Sortable.min.js"></script>
    <script>
        // Initialize drag and drop
        const tbody = document.getElementById('bannersList');
        const sortable = Sortable.create(tbody, {
            animation: 150,
            handle: '.drag-handle',
            onEnd: function (evt) {
                // Get new order
                const rows = tbody.querySelectorAll('tr');
                const bannerIds = [];
                rows.forEach(row => {
                    bannerIds.push(parseInt(row.dataset.id));
                });

                // Send to server
                fetch('/Banners/UpdateOrder', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(bannerIds)
                })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        // Update display order numbers
                        rows.forEach((row, index) => {
                            row.querySelector('td:nth-child(2)').textContent = index + 1;
                        });
                    }
                })
                .catch(error => console.error('Error:', error));
            }
        });
    </script>
}

